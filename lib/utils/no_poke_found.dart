import 'package:flutter/material.dart';

class NoPokeFound extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("No poke list found.")),
    );
  }
}
